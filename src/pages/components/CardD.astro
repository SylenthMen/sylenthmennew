<div class="card">
  <img
    class="w-60 h-60 justify-center items-center flex flex-col"
    src="/discord.svg"
    alt="logotipo discord"
  />
  <div class="card__content">
    <p class="card__title">unete a mi discord</p>
    <p class="card__description mb-4">
      Únete a nuestra comunidad en Discord, donde la diversión y la interacción
      no paran. Conéctate con otros seguidores, participa en conversaciones
      exclusivas, y mantente al día con todas nuestras novedades y eventos. ¡No
      esperes más y únete a sylenthmen en Discord hoy mismo!
    </p>
    <div class="mt-4 pt-2">
      <a
        target="_blank"
        class="mt-8 p-4 bg-sky-950 rounded-md capitalize"
        href="https://discord.gg/87CxhjNmUf">unirme a discord</a
      >
    </div>
  </div>
</div>
<style>
  :root {
    --colorCardD: #959df5;
    --colorBannerD: #96a4d9;
    --sombritaD: rgba(0, 63, 237, 0.2);
  }
  .card {
    position: relative;
    width: 300px;
    height: 320px;
    background-color: var(--colorCardD);
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    perspective: 1000px;
    transition: all 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .card:hover {
    transform: scale(1.05);
    box-shadow: 0 8px 16px var(--sombritaD);
  }

  .card__content {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    padding: 20px;
    box-sizing: border-box;
    background-color: var(--colorBannerD);
    transform: rotateX(-90deg);
    transform-origin: bottom;
    transition: all 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .card:hover .card__content {
    transform: rotateX(0deg);
  }

  .card__title {
    margin: 0;
    font-size: 24px;
    color: #fff;
    font-weight: bold;
    text-transform: capitalize;
  }

  .card:hover svg {
    scale: 0;
  }

  .card__description {
    margin: 10px 0 0;
    font-size: 14px;
    color: #fff;
    line-height: 1.4;
  }
</style>
